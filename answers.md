## Task 1 Command: awk -F',' 'NR > 1 { count++ } END { print count }' Lab03-data.csv Result: 322 Explanation: The field separator is set to a comma. It checks if the current line number is greater than 1 to skip the header, increments a counter for each record, and prints the total at the end.
## Task 2 Command: awk -F',' 'NR > 1 { seen[$1] = 1 } END { print length(seen) }' Lab03-data.csv Result: 14 Explanation: It creates an associative array named 'seen' using student names as unique keys to filter out duplicates. The length function in the END block prints the total number of unique keys stored.
## Task 3 Command: awk -F',' 'NR > 1 { seen[$3] = 1 } END { print length(seen) }' Lab03-data.csv Result: 23 Explanation: It populates an associative array using the assignment column as unique keys to eliminate duplicates. The length function in the END block computes and prints the total number of unique assignments found.
## Task 4 Command: awk -F',' 'NR > 1 && $3 == "Q01" { count++ } END { print count }' Lab03-data.csv Result: 14 Explanation: It checks if the current line number is greater than 1 to skip the header and uses the logical AND operator to verify if the third column equals Q01, incrementing a counter for each match to print the total at the end.
## Task 5 Command: awk -F',' -f task5.awk Lab03-data.csv Result: 14.2857 Explanation: The script filters records where the third column is Q01, accumulates the scores from the fourth column into a sum variable, and increments a counter. In the END block, it divides the total sum by the counter to calculate and print the average score.
## Task 6 Command: awk -F',' -f task6.awk Lab03-data.csv Result: Tomas: 82.22% (B), Diana: 62.08% (D), Andrew: 73.69% (C)... Explanation: It uses two associative arrays to accumulate points earned and points possible per student. In the END block, it calculates the weighted percentage for each student, passes it through an if/else if chain to assign the correct letter grade based on the scale, and prints the formatted output.